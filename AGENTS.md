# Repository conventions

Use lowercase `bb` for the product name in prose, comments, user-facing text,
and agent instructions, including at the start of a sentence. Preserve exact
casing for SDK identifiers, environment variables, and historical message
formats required for reconciliation.
